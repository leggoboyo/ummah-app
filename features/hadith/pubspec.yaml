name: hadith
description: Placeholder package for the hadith library module.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  core:
    path: ../../packages/core
  http: ^1.6.0
  path: ^1.9.1
  path_provider: ^2.1.5
  sqlite3: ^3.2.0
  sqlite3_flutter_libs: ^0.6.0+eol

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1
  test: ^1.25.8
