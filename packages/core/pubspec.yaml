name: core
description: Shared core models, ports, and privacy-first infrastructure contracts.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
