name: scholar_feed
description: Curated scholar/article feed sources with local metadata caching.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  core:
    path: ../../packages/core
  http: ^1.6.0
  xml: ^6.5.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
