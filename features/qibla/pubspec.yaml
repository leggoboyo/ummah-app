name: qibla
description: Qibla bearing math and compass-ready models.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  core:
    path: ../../packages/core

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.8
